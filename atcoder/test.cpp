#include <bits/stdc++.h>
using namespace std;

int main()
{
    string ch;
    cin >> ch;
    // int x = atoi(ch.at(0).c_str());
    // cout << x << endl;
    int y = (int)(ch.at(0) - '0');
    cout << y << endl;
}
