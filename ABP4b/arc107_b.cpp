#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, k;
    cin >> n >> k;
    int64_t count = 0;
    for (int i = 2; i <= n * 2; i++)
    {
        for (int j = 2; j <= n * 2; j++)
        {
            if (i - j == k)
            {
            }
        }
    }
    cout << count << endl;
}
